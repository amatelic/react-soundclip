import { describe, it } from "vitest";
import { render, screen } from "@testing-library/react";
import { AudioPresent } from "../src/index";

describe("Test the context lookp", () => {
  it("it render", () => {
    render(null);
    // Test implementation
  });
});
